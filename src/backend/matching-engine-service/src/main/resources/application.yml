spring:
  application:
    name: matching-engine-service
  kafka:
    bootstrap-servers: ${SPRING_KAFKA_BOOTSTRAP_SERVERS:localhost:9092}

grpc:
  server:
    port: ${GRPC_SERVER_PORT:9007}

logging:
  level:
    com.ordermatching: DEBUG
    org.springframework.kafka: WARN
